services:
  2step-converter:
    build: .
    image: 2step-converter:latest
    container_name: 2step-converter
    ports:
      - "8000:8000"
    volumes:
      # Persist converted files across restarts
      - ./uploads:/app/uploads
      - ./output:/app/output
    restart: unless-stopped
